name: jevis_flutter
description: Autonomous goal-driven Flutter UI tests. Define actions once and let Jevis select actions and evaluate completion.
version: 0.1.0
environment:
  sdk: '>=3.4.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  http: ^1.2.0
dev_dependencies:
  flutter_lints: ^5.0.0
